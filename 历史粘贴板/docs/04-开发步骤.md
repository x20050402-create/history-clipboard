# 04 · 开发步骤（路线图）

> 原则：每一小步都能独立运行、独立验收，验收通过再进下一步；每步结束更新当天开发日志。
> 状态标记：`[ ]` 待办 · `[~]` 进行中 · `[x]` 完成

---

## 阶段 0 · 项目治理
- [x] 0.1 创建 docs/（5 份标准文件）
- [x] 0.2 创建 开发日志/（按日期）
- [x] 0.3 创建 CLAUDE.md（总入口）

**验收**：目录结构齐全，CLAUDE.md 能索引到全部文件。

---

## 阶段 1 · 可运行骨架
- [x] 1.1 安装依赖（`npm install`），跑通最小空窗口（`npm start`）
- [x] 1.2 系统托盘：左键开关窗口、关闭=隐藏、右键退出（代码就绪，待用户实机验收）

**验收**：`npm start` 弹出窗口；托盘出现图标，点击能开关窗口，右键能退出。
**体验方式**：在 VS Code 终端运行 `npm start`（详见 CLAUDE.md 运行说明）。

---

## 阶段 2 · 数据与监听（逐项联调）
- [ ] 2.1 数据库建表 + 增删查冒烟测试
- [ ] 2.2 监听并记录"文字"
- [ ] 2.3 记录"图片"（存文件 + 库存文件名）
- [ ] 2.4 记录"文件/文件夹"路径
- [ ] 2.5 去重 + 跳过敏感内容
- [ ] 2.6 自动清理（保存天数 + 200 条上限）

**验收**：复制文字/图片/文件后数据被正确记录；重复内容只记一条；密码不记录；过期/超量被清理。

---

## 阶段 3 · 界面
- [ ] 3.1 静态布局 + 淡蓝主题（按 `03-设计规范.md`）
- [ ] 3.2 列表渲染（IPC 取数据、时间倒序、置顶区在上）
- [ ] 3.3 卡片操作：点击复制 / 置顶 / 删除
- [ ] 3.4 顶部搜索实时筛选
- [ ] 3.5 "软件需运行才会记录"提示 + 空状态

**验收**：窗口看到历史卡片，能搜、能置顶、能删、点击能复制粘贴。

---

## 阶段 4 · 设置与打包
- [ ] 4.1 设置面板：保存天数档位（1/3/5/7/30/永久）
- [ ] 4.2 应用/托盘图标（淡蓝剪贴板图标）
- [ ] 4.3 electron-builder 打包 .exe，安装验证

**验收**：设置可改并立即生效；生成 .exe，安装后桌面有图标、双击可用。

---

## 当前进度

- 已完成：阶段 0（项目治理）
- 下一步：**阶段 1.1** 安装依赖、跑通空窗口
